---
- name: setup webservers
  hosts: webservers

  tasks:
  - name: install apache
    yum:
      name: httpd
      state: present

  - name: start httpd
    service:
      name: httpd
      state: started

  - name: copy index.html
    template:
      src: index.j2
